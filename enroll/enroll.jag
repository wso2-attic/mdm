<%
    include('/commans/ipconf.jag');
    var log = new Log();
    var userAgent= request.getHeader("User-Agent");

    var android = userAgent.indexOf("Android");

    if(android>0){
        response.sendRedirect(HTTP_URL+"/mdm/mdm.apk");
    }else{
        response.sendRedirect( RUBY_SERVER_URL);
    }
%>
